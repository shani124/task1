import { ShortentextcustompipePipe } from './shortentextcustompipe.pipe';

describe('ShortentextcustompipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortentextcustompipePipe();
    expect(pipe).toBeTruthy();
  });
});
